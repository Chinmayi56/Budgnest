# This is a pointer file. The actual environment variables used by the
# backend live in backend/.env.example — copy that file to backend/.env
# and fill in your local values.
#
# See backend/.env.example for:
#   MONGO_URL, DB_NAME, JWT_SECRET, JWT_ALGORITHM, JWT_EXPIRE_MINUTES, CORS_ORIGINS
